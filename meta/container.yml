symfony:
  image: "centos:7"
  user: symfony
  ports:
  - 8000:8000
  command: ["/bin/true"]
  working_dir: /project
  volumes:
  - ..:/project
  environment:
  - PROJECT_NAME=example
  entry_point: ["/usr/bin/entrypoint.sh"]

  dev_overrides:
     command: ["php", "bin/console", "server:run"]   
